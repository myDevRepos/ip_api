# Instructions

Since you can download this source code, you are a business subscriber to `ipapi.is`.

Please login to your account at `https://ipapi.is/app/` and find instructions how to self-host `ipapi.is` in the dashboard.

In the dashboard, you will also find a download section that gives you access to the full `ipapi.is` database in CSV format.
